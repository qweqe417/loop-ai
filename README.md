# AI Coding Loop

This repository hosts the skeleton for the AI Coding Loop system.

Current focus:

- keep `core` tool-agnostic
- expose capabilities through tool-specific plugins
- preserve provider and scenario verification extension points

See `docs/architecture/project-structure.md` for the current directory layout.
